const USERNAME_TAKEN = 'There\'s already a user with this username';
const EMAIL_TAKEN = 'Account with this email already registered';
const INVALID_EMAIL = 'Invalid email';
const INVALID_USERNAME = 'Username must only be with letters and not shorter than 5 symbols';
const INVALID_PASSWORD = 'Password must not be shorter than 8 symbols';
const FIELD_REQUIRED = 'This field is required';
const PASSWORD_REQUIRED = 'Enter a password';
const WRONG_SYMBOLS = '[A-Z], [0-9] symbols only';
const WRONG_PASSWORD = 'Wrong password';
const TOKEN_NOT_PROVIDED = 'Token not provided';
const USER_NOT_FOUND = 'There is no such user';
const QUESTION_NOT_FOUND = 'Question not found';
const ANSWER_NOT_FOUND = 'Answer not found';

module.exports = {
  USERNAME_TAKEN,
  EMAIL_TAKEN,
  INVALID_EMAIL,
  INVALID_USERNAME,
  INVALID_PASSWORD,
  FIELD_REQUIRED,
  PASSWORD_REQUIRED,
  WRONG_SYMBOLS,
  WRONG_PASSWORD,
  TOKEN_NOT_PROVIDED,
  USER_NOT_FOUND,
  QUESTION_NOT_FOUND,
  ANSWER_NOT_FOUND
};
